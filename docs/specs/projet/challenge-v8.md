# Challenge de la spec v8 (spec-challenger, 2026-09-23)

Périmètre : vague 2 (T-18a à T-23, ADR-19/20/21). Verdict : à retravailler. Deux bloquants (#1, #2) et
un arbitrage FinOps (#15), tranchés par l'utilisateur dans `docs/specs/2026-09-23-vague-2-ui/interview.md`.

| # | Sév. | Lentille | Trou | Proposition |
| --- | --- | --- | --- | --- |
| 1 | bloquant | Perf/UX | Onglet caché > 60 s : `appliquerDelta` passe en forme fermée et crédite l'or sans plafond H, sans combat, sans résumé (moteur.ts:283-285). Fermé = plafond H, caché 20 h = 20 h. | Tranché : traité comme hors-ligne. |
| 2 | bloquant | UX/Sécu | Comportement de l'onglet secondaire non défini ; pas de reprise de main. | Tranché : figé + relais automatique par relecture du stockage. |
| 3 | majeur | Testabilité | Aucune fabrique injectable pour `src/state/`, ordre de démarrage non écrit. Piège : le principal contient le **base64** d'`exporterTexte`, pas un JSON ; le relire via `deserialiser(JSON.parse(...))` déclencherait EXG-27 à chaque chargement (index.ts:181 vs :236). | `creerStoreJeu({ horloge, stockage, canal, matchMedia, idOnglet, etatInitial })` ; démarrage : verrou → `importerTexte(principal)` sans exécuter son plan → `calculHorsLigne` (propriétaire) → sauvegarde immédiate → boucle → auto-sauvegarde + battement. Format du principal = `exporterTexte` partout. |
| 4 | majeur | Testabilité | EXG-22/23 restent verts si l'on sauvegarde à chaque tick. | Compter les écritures : 0 à 29,9 s, 1 à 31 s ; 0→1 sur `hidden` ; 0 dans l'onglet secondaire. Ajouter `pagehide` (Safari mobile). |
| 5 | majeur | Sécu | Transport du verrou : course à l'ouverture simultanée, ralentissement des timers en arrière-plan, crash sans `liberer`, `liberer` sur `hidden`. | Ping `BroadcastChannel` au démarrage (~500 ms) ; écrire puis relire (le dernier écrit gagne) ; expiration ≥ 3 × cadence et > ralentissement d'arrière-plan ; relire le verrou avant chaque écriture ; `liberer` sur `pagehide`, re-revendiquer sur `pageshow`. |
| 6 | majeur | Sécu/UX | EXG-27 sans test de done ; l'auto-sauvegarde pendant l'écran d'erreur écrase la donnée corrompue ; « nouvelle partie » copierait le principal corrompu sur un `.bak` valide. | Principal tronqué → message, stockage identique octet pour octet après 31 s et `hidden` ; proposer « restaurer .bak » si valide ; ne jamais écraser un `.bak` valide par un principal illisible. |
| 7 | majeur | Ordre | Test EXG-46 dépend de l'UI d'import (T-28, vague 4) ; restauration irréversible et crédit hors-ligne non définis. | Action de store `importer(texte)` testée sans UI en T-23 ; restauration confirmée, `derniereSauvegardeMs = maintenant`, sans crédit hors-ligne. |
| 8 | majeur | Sécu | EXG-47 sans preuve en vague 2 ; les messages d'`ErreurImport` portent des chemins contrôlés par l'attaquant. | Test navigateur (nom hostile et message d'erreur → texte littéral, pas d'`img`, `window.__x` indéfini) + grep `dangerouslySetInnerHTML|innerHTML|insertAdjacentHTML` dans `verify.sh`, vu rouge. |
| 9 | majeur | Sécu | Origine GitHub Pages partagée entre dépôts : clés non préfixées ; `reglages` peu cadrée. | Préfixe `magic-battle:` (clés et canal) ; `reglages` : try/catch, seul `performance` booléen lu, jamais d'étalement ; absent = suit la media query ; fixtures hostiles vues rouges. |
| 10 | majeur | Test/Perf | Test de renders vert sans témoin positif ; `setState` à chaque rAF même à 0 tick ; EXG-2 non testé côté UI. | `<Profiler>` ; témoin : l'or change après 10 ticks ; panneau indépendant = 0 commit ; delta 5 s = 1 notification ; pas de `setState` si `nbTicks = 0` ; sélecteurs rendant des primitives. |
| 11 | majeur | Test/UX | EXG-53 : `fireEvent`/user-event ne voient pas un recouvrement ; un `Dialog` shadcn bloque la page. | Clics réels Playwright ou `elementFromPoint` ; encart `role="status"`, pas `Dialog` ; pas de fermeture auto (ou ≥ 20 s avec pause) ; affichage si Δt ≥ 60 s. |
| 12 | majeur | Test/A11y | axe classe les contrastes douteux en `incomplete` ; un seul écran audité ; « test visuel passe » sans assertion ; `aria-live` à 10 Hz. | violations = 0 et `incomplete` `color-contrast` = 0 sur la liste d'états ; assertions de mise en page 1440/375 ; toutes les cibles mesurées ; miroir DOM sans `aria-live` continu. |
| 13 | majeur | Perf/Test | Aucune source de projectiles côté moteur ; test de plafond vert si le scénario ne sature pas ; 4 ms en done alors qu'EXG-52 le dit informatif ; EXG-30 sans critère. | Effets dérivés des écarts de compteurs entre frames ; `N_PROJECTILES_MAX` dans `src/canvas/` ; test demandes > 200, dessins ≤ 200, plus anciens supprimés ; 4 ms informatif. |
| 14 | majeur | UX | Confirmations : gain mouvant pendant la modale, touches 1-6 actives, focus initial, annulation et Ascension non testées, boutons actifs après fin de partie. | Crédité = affiché au clic ; raccourcis ignorés sous modale ou champ ; focus sur « Annuler » ; tests annulation et Ascension à 0 ; désactivés si `partieTerminee`. |
| 15 | majeur | FinOps | T-23 en Sonnet malgré §11 ; estimation non recalée ; `implementeur-ui` absent ; Chromium en CI et hook Stop sans budget ; tests navigateur IGNORÉS en `--strict`. | Tranché : T-23a Opus / T-23b Sonnet. Reste : créer l'agent avant T-18a, cache Playwright en CI, budget de temps, absence de Chromium = ÉCHEC en strict, recaler §11. |

Non vérifié par le challenger : comportement de `vi.useFakeTimers` et de l'émulation `prefers-reduced-motion`
en browser mode ; chiffres de ralentissement des timers de Chrome ; périmètre exact d'`equilibrage:empreinte`
(où peuvent vivre les textes d'UI de la vague 2).
