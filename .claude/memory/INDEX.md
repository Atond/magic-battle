# Mémoire de projet — idlev1

Quatre fichiers, pas plus (plan d'intégrations, 2026-09-20). Les décisions (ADR) vivent dans
`docs/specs/projet/spec.md` §15 — source unique, jamais dupliquée ici.

- `BLOCKERS.md` — murs rencontrés (Ouvert / Résolu). On l'écrit dès qu'on bute.
- `LEARNINGS.md` — apprentissages issus des blocages résolus. Monte en skill quand ça se répète.
- `EVALS.md` — ce qu'on mesure : chiffres du rapport `idle-balance` (1er prestige, murs, couple
  Ascensions × prestiges, H, durée cumulée), budget de frame, taille de sauvegarde.

Convention : `BLK-n` → `LRN-n` ; date ISO ; une entrée = un fait.
